# The log directory
logdir: logs

scheduler:
  # Keep building and find all the errors
  on-error: continue
  # Go easy on the number of parallel builds
  builders: 1

# Get a lot of output in case of errors
logging:
  verbose: False
  error-lines: 80
